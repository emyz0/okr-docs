/**
 * 📄 PDF GÖRSELLERİ İŞLEME
 * 
 * UYARI: Bu dosya şu anda kullanılmıyor.
 * LangChain PDFLoader'ı görsel metni otomatik çıkartıyor.
 * 
 * Gelecekte gerekli olursa aktivasyon için:
 * - pdfjs-dist kütüphanesi kurulmalı
 * - Canvas rendering uygulanmalı
 * - Tesseract.js OCR entegrasyonu yapılmalı
 * 
 * İMDİ: lib/rag/image-processing.ts kullanılıyor (Tesseract OCR)
 */

// Not: Bu dosya TypeScript compile hatası vermemesin diye sadeleştirildi
// Üretim kodu upload/route.ts'te has_images flag'i ile yeterli
